## Automated Data Sender (fa_automated_data_sender)


This function app serves as general tool for reporting with flow like:

Obtain data from database (MSSQL/PostgreSQL) -> Export as excel/CSV file -> Send to recipient -> Delete processed file